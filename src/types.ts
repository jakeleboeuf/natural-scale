export interface ScaleTypes {
  interval: any;
  unit?: string | undefined;
  base?: string | number;
}

// export interface ScaleTypes {
//   step: number;
//   options?: ScaleOptions;
// }

export interface ScaleOptions {
  unit?: string;
  interval?: number;
}
